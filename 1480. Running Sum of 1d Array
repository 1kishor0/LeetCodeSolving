class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        vector<int> runningsums;
        int n=0;
        for(int i=0;i<nums.size();++i){
            n=n+nums[i];
            runningsums.push_back(n);
        }
        return runningsums;
    }
};
